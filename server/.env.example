# 后端环境变量示例
# 复制本文件为 .env，填入实际值后本地启动服务
# 注意：.env 文件不会被提交到 git

# 服务端口（可选，默认 3000）
PORT=3000

# CloudBase 环境 ID（必填）
TCB_ENV_ID=your-cloudbase-env-id

# 腾讯云密钥（本地开发时填写，云托管环境内置身份可留空）
TCB_SECRET_ID=
TCB_SECRET_KEY=
